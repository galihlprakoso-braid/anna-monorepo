# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true

# Security (set to false in production)
BYPASS_AUTH=true
BYPASS_CORS=true

# Authentication (only used when BYPASS_AUTH=true)
DEFAULT_USER_ID=hardcoded-user-123
